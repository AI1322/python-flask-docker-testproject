sudo docker logs -f web
sudo docker top web
sudo docker run -it --name lab-ubuntu ubuntu:24.04 bash
root@45783ba310c1:/# sudo apt-get update && apt-get install -y curl	
root@45783ba310c1:/# mkdir -p /opt && echo "lab image v1" >/opt/banner.txt
root@45783ba310c1:/# cat /opt/banner.txt
root@45783ba310c1:/# exit
anton@ant:~$ sudo docker commit lab-ubuntu lab-ubuntu:curl
sudo docker run lab-ubuntu:curl
sudo apt install python3-pip

make app
cd /app
sudo app install python3-flask
sudo apt install python3.12-venv
source venv/bin/activate
pip install Flask

sudo docker build -t demo-python:1 .
sudo docker run -d -p 8000:8000 --name demo-container demo-python:11~sudo docker logs -f web
sudo docker top web
sudo docker run -it --name lab-ubuntu ubuntu:24.04 bash
root@45783ba310c1:/# sudo apt-get update && apt-get install -y curl	
root@45783ba310c1:/# mkdir -p /opt && echo "lab image v1" >/opt/banner.txt
root@45783ba310c1:/# cat /opt/banner.txt
root@45783ba310c1:/# exit
anton@ant:~$ sudo docker commit lab-ubuntu lab-ubuntu:curl
sudo docker run lab-ubuntu:curl
sudo apt install python3-pip

make app
cd /app
sudo app install python3-flask
sudo apt install python3.12-venv
source venv/bin/activate
pip install Flask

sudo docker build -t demo-python:1 .
sudo docker run -d -p 8000:8000 --name demo-container demo-python:1
